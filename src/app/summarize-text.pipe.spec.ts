import { SummarizeTextPipe } from './summarize-text.pipe';

describe('SummarizeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SummarizeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
